<nav>
    {% set subNav = collections.all | eleventyNavigation %}
    <ul>
        {% for item in subNav %}
            <li>
                <a href="{{ item.url | url }}"{% if item.url == page.url %} data-current="current item"{% endif %}>{{ item.title }}</a>
                <pre>
<code>
    {{ item | dump }}
</code>
</pre>

            </li>
        {% endfor %}
    </ul>
</nav>