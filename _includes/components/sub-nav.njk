{% set navTitle = subMenu.navItems.navtitle %}
{% set subNav = subMenu.navItems %}

{% for item in subNav %}

    <nav>
        <ul>
            {% for link in item.navItem.linkObject %}
                <li>
                    <a href="{{ link.url | url }}"{% if link.url == page.url %} data-current="current item"{% endif %}>{{ link.displayText }}</a>
                </li>
            {% endfor %}
        </ul>
    </nav>
{% endfor %}