<h2>Sub nav</h2>
{% set subPages = collections.pages | eleventyNavigation(parent) %}
<ul>
    {% for page in subPages %}
        <li>
            <a href="{{ page.url | url }}">
                {{ page.title }}
            </a>
        </li>
    {% endfor %}
</ul>